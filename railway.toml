# Root Directory in Railway must be: server
# Leave "Custom Build Command" empty in the dashboard (nixpacks.toml runs npm ci).
[build]
builder = "NIXPACKS"

[deploy]
startCommand = "npm start"
healthcheckPath = "/api/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
