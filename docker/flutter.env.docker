# Baked into the Flutter web build. PUBLIC_ORIGIN is substituted at image build time.
API_BASE_URL=__PUBLIC_ORIGIN__/api
SOCKET_URL=__PUBLIC_ORIGIN__
SHARE_WEB_BASE_URL=__PUBLIC_ORIGIN__
APP_NAME=NewsNow
APP_ENV=production
