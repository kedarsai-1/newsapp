# Nixpacks (Railway). Do not set a custom Build Command in the dashboard — this file handles install.
[variables]
NPM_CONFIG_CACHE = "/tmp/npm-cache"
NODE_ENV = "production"
# Avoid heavy RSS/YouTube/ML during deploy on small Railway instances (512MB).
SCRAPER_RUN_ON_START = "false"
YOUTUBE_RUN_ON_START = "false"
POLITICAL_VIDEO_RUN_ON_START = "false"
POLITICAL_ML_PRELOAD = "false"
POLITICAL_ML_ENABLED = "false"
INGEST_MAX_RUNTIME_MS = "360000"

[phases.install]
cmds = ["npm ci --omit=dev"]

[start]
cmd = "npm start"
